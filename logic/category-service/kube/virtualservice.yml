apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: category-service
spec:
  hosts:
    - category-service
  gateways:
    - kong-gateway
  http:
    - route:
        - destination:
            host: category-service
            port:
              number: 80
